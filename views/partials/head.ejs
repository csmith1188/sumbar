<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sumbar</title>
</head>

<body>
  <div id="userbar">
    <% if (token) { %>
    <%= token.username %> ||
    <% if (token.permissions >= 4) { %>
    <a href="/teacher">Teacher</a> ||
    <% } else { %>
    <a href="/student">Student</a> ||
    <% } %>
    <a href="/logout">Logout</a>
    <% } else { %>
    <a href="/login">Login</a>
    <% } %>
  </div>